services:
  - type: web
    name: mindscape-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 8000
    pythonVersion: "3.11"  # Ensure the correct Python version
